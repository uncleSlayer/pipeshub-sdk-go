// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package components

type SemanticSearchHistoryFiltersAvailablePagination struct {
	Page  SemanticSearchHistoryPaginationField `json:"page"`
	Limit SemanticSearchHistoryPaginationField `json:"limit"`
}

func (s *SemanticSearchHistoryFiltersAvailablePagination) GetPage() SemanticSearchHistoryPaginationField {
	if s == nil {
		return SemanticSearchHistoryPaginationField{}
	}
	return s.Page
}

func (s *SemanticSearchHistoryFiltersAvailablePagination) GetLimit() SemanticSearchHistoryPaginationField {
	if s == nil {
		return SemanticSearchHistoryPaginationField{}
	}
	return s.Limit
}

type Sorting struct {
	// Used for `available.sorting.{sortBy,sortOrder}` and
	// `available.sortingMessages.{sortBy,sortOrder}`. The `applied` flag
	// is present on `sorting.*` and absent on `sortingMessages.*`, so it
	// is optional here.
	//
	SortBy SemanticSearchHistorySortField `json:"sortBy"`
	// Used for `available.sorting.{sortBy,sortOrder}` and
	// `available.sortingMessages.{sortBy,sortOrder}`. The `applied` flag
	// is present on `sorting.*` and absent on `sortingMessages.*`, so it
	// is optional here.
	//
	SortOrder SemanticSearchHistorySortField `json:"sortOrder"`
}

func (s *Sorting) GetSortBy() SemanticSearchHistorySortField {
	if s == nil {
		return SemanticSearchHistorySortField{}
	}
	return s.SortBy
}

func (s *Sorting) GetSortOrder() SemanticSearchHistorySortField {
	if s == nil {
		return SemanticSearchHistorySortField{}
	}
	return s.SortOrder
}

type DateFilters struct {
	DateRange SemanticSearchHistoryDateRange `json:"dateRange"`
}

func (d *DateFilters) GetDateRange() SemanticSearchHistoryDateRange {
	if d == nil {
		return SemanticSearchHistoryDateRange{}
	}
	return d.DateRange
}

type MessageFilters struct {
	// Generic "filter X is available, current value is Y" block used for
	// `shared`, `tags`, `minMessages`, `search`, and `messageType`. Either
	// `type` (free-form value) or `values` (enum of allowed strings) is
	// present, not both. `current` is the caller-supplied value passed
	// through from `req.query`, hence string-or-null even when `type` is
	// `'number'`.
	//
	MessageType SemanticSearchHistoryFilterToggle `json:"messageType"`
}

func (m *MessageFilters) GetMessageType() SemanticSearchHistoryFilterToggle {
	if m == nil {
		return SemanticSearchHistoryFilterToggle{}
	}
	return m.MessageType
}

type SortingMessages struct {
	// Used for `available.sorting.{sortBy,sortOrder}` and
	// `available.sortingMessages.{sortBy,sortOrder}`. The `applied` flag
	// is present on `sorting.*` and absent on `sortingMessages.*`, so it
	// is optional here.
	//
	SortBy SemanticSearchHistorySortField `json:"sortBy"`
	// Used for `available.sorting.{sortBy,sortOrder}` and
	// `available.sortingMessages.{sortBy,sortOrder}`. The `applied` flag
	// is present on `sorting.*` and absent on `sortingMessages.*`, so it
	// is optional here.
	//
	SortOrder SemanticSearchHistorySortField `json:"sortOrder"`
}

func (s *SortingMessages) GetSortBy() SemanticSearchHistorySortField {
	if s == nil {
		return SemanticSearchHistorySortField{}
	}
	return s.SortBy
}

func (s *SortingMessages) GetSortOrder() SemanticSearchHistorySortField {
	if s == nil {
		return SemanticSearchHistorySortField{}
	}
	return s.SortOrder
}

// SemanticSearchHistoryFiltersAvailable - Catalogue of filters the endpoint supports, plus their current values
// and `applied` flags. Built by `buildFiltersMetadata` (utils.ts:430-624).
type SemanticSearchHistoryFiltersAvailable struct {
	// Generic "filter X is available, current value is Y" block used for
	// `shared`, `tags`, `minMessages`, `search`, and `messageType`. Either
	// `type` (free-form value) or `values` (enum of allowed strings) is
	// present, not both. `current` is the caller-supplied value passed
	// through from `req.query`, hence string-or-null even when `type` is
	// `'number'`.
	//
	Shared SemanticSearchHistoryFilterToggle `json:"shared"`
	// Generic "filter X is available, current value is Y" block used for
	// `shared`, `tags`, `minMessages`, `search`, and `messageType`. Either
	// `type` (free-form value) or `values` (enum of allowed strings) is
	// present, not both. `current` is the caller-supplied value passed
	// through from `req.query`, hence string-or-null even when `type` is
	// `'number'`.
	//
	Tags SemanticSearchHistoryFilterToggle `json:"tags"`
	// Generic "filter X is available, current value is Y" block used for
	// `shared`, `tags`, `minMessages`, `search`, and `messageType`. Either
	// `type` (free-form value) or `values` (enum of allowed strings) is
	// present, not both. `current` is the caller-supplied value passed
	// through from `req.query`, hence string-or-null even when `type` is
	// `'number'`.
	//
	MinMessages SemanticSearchHistoryFilterToggle `json:"minMessages"`
	// Generic "filter X is available, current value is Y" block used for
	// `shared`, `tags`, `minMessages`, `search`, and `messageType`. Either
	// `type` (free-form value) or `values` (enum of allowed strings) is
	// present, not both. `current` is the caller-supplied value passed
	// through from `req.query`, hence string-or-null even when `type` is
	// `'number'`.
	//
	Search          SemanticSearchHistoryFilterToggle               `json:"search"`
	Pagination      SemanticSearchHistoryFiltersAvailablePagination `json:"pagination"`
	Sorting         Sorting                                         `json:"sorting"`
	DateFilters     DateFilters                                     `json:"dateFilters"`
	MessageFilters  MessageFilters                                  `json:"messageFilters"`
	SortingMessages SortingMessages                                 `json:"sortingMessages"`
}

func (s *SemanticSearchHistoryFiltersAvailable) GetShared() SemanticSearchHistoryFilterToggle {
	if s == nil {
		return SemanticSearchHistoryFilterToggle{}
	}
	return s.Shared
}

func (s *SemanticSearchHistoryFiltersAvailable) GetTags() SemanticSearchHistoryFilterToggle {
	if s == nil {
		return SemanticSearchHistoryFilterToggle{}
	}
	return s.Tags
}

func (s *SemanticSearchHistoryFiltersAvailable) GetMinMessages() SemanticSearchHistoryFilterToggle {
	if s == nil {
		return SemanticSearchHistoryFilterToggle{}
	}
	return s.MinMessages
}

func (s *SemanticSearchHistoryFiltersAvailable) GetSearch() SemanticSearchHistoryFilterToggle {
	if s == nil {
		return SemanticSearchHistoryFilterToggle{}
	}
	return s.Search
}

func (s *SemanticSearchHistoryFiltersAvailable) GetPagination() SemanticSearchHistoryFiltersAvailablePagination {
	if s == nil {
		return SemanticSearchHistoryFiltersAvailablePagination{}
	}
	return s.Pagination
}

func (s *SemanticSearchHistoryFiltersAvailable) GetSorting() Sorting {
	if s == nil {
		return Sorting{}
	}
	return s.Sorting
}

func (s *SemanticSearchHistoryFiltersAvailable) GetDateFilters() DateFilters {
	if s == nil {
		return DateFilters{}
	}
	return s.DateFilters
}

func (s *SemanticSearchHistoryFiltersAvailable) GetMessageFilters() MessageFilters {
	if s == nil {
		return MessageFilters{}
	}
	return s.MessageFilters
}

func (s *SemanticSearchHistoryFiltersAvailable) GetSortingMessages() SortingMessages {
	if s == nil {
		return SortingMessages{}
	}
	return s.SortingMessages
}
